import React from 'react';

export const StudyContext = React.createContext(); //the React.createContext() method comes with a Consumer and Provider React component
                                          //that needs to be exported. 

// const StudyProvider = StudyContext.Provider //grabs the Provider from React.createContext().
// const StudyConsumer = StudyContext.Consumer//grabs the Consumer from React.createContext(). 

// export { StudyProvider, StudyConsumer }

